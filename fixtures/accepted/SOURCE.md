# Accepted fixture: migration command

ReleaseProof adds a `releaseproof migrate` command that guides maintainers through
the required repository migration steps. The command is intentionally documented as
a testnet-only release workflow and does not execute any external-chain action.
